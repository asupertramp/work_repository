#include<stdio.h>#include<malloc.h>typedef int ElementType;typedef struct {	struct Node *next;	ElementType val;}Node;typedef  Node* Head;typedef Node* List;Head creat(){	Head list=malloc(sizeof(Node));	if(list!=NULL)		list->next=NULL;	return list;}int insert_L(List *list,ElementType x){	Node *new=malloc(sizeof(Node));	if(new==NULL)		return 1;	new->val=x;	new->next=*list;	*list=new;	return 0;}int insert(Head list,ElementType x){	Node *new;	if(list==NULL)		return 1;	new=(Node*)malloc(sizeof(Node));	if(new==NULL)	{		printf("Error\n");		return 1;	}	new->val=x;	new->next=list->next;	list->next=new;	return 0;}void delete_L(List *list,ElementType x){	}void delete(Head list,ElementType x){	Node * pre=list;	while((list=list->next)!=NULL)	{		if(list->val==x)		{			pre->next=list->next;			free(list);			return;		}		pre=list;	}}Node *find(Head list,ElementType x){	while((list=list->next)!=0)	{		if(list->val==x)			return list;	}	return NULL;}void destroy(Head list){	Node *tmp=list;	while((list=list->next)!=NULL)	{		free(tmp);		tmp=list;	}}void traverse(Head list){	Node *tmp;	if(list==NULL)		return;	tmp=list->next;		while(tmp!=NULL)	{		printf("%d ",tmp->val);		tmp=tmp->next;	}	printf("\n");}void test(){	int i;	List l=NULL;	for(i=0;i<10;++i)		insert_L(&l,i);	traverse(l);	/*	Head list=creat();	if(list==NULL)		return;	for(i=0;i<10;++i)	{		insert(list,i);	}	traverse(list);	delete(list,5);	traverse(list);	delete(list,9);	traverse(list);	traverse(find(list,4));	destroy(list);	traverse(list);	*/}int main(){	test();}